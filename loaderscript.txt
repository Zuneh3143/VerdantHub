local Scripts = {
    "https://raw.githubusercontent.com/Zuneh3143/Nhamnhi/main/Forsaken",
   
}

for _, url in ipairs(Scripts) do
    local success, err = pcall(function()
        loadstring(game:HttpGet(url))()
    end)
    
    if success then
        print("[✓] Loaded: " .. url)
    else
        warn("[✗] Failed to load: " .. url .. " | Error: " .. tostring(err))
    end
end
